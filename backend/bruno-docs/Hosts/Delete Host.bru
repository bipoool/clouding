meta {
  name: Delete Host
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/hosts/:id
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{authToken}}
}

docs {
  Delete a host by ID.
}
