meta {
  name: Get All SSH Credentials
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/credentials
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{authToken}}
}

docs {
  List all SSH credentials owned by the authenticated user.
}
